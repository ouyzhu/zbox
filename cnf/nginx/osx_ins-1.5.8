ins_src_addr		= http://nginx.org/download/nginx-1.5.8.tar.gz

# need to override this, since "--with-file-aio" only works on FreeBSD or linux
ins_make_configure_opts	= "--prefix=ZBOX_INS_FULLPATH --with-http_ssl_module --with-http_realip_module"
