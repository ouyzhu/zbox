# GOOD: fastest way to install via macports@osx
# BAD: unison usually need version match, but macports by default only maitains single version for big osx version

ins_steps       = "dep"
ins_dep_install	= "unison"

