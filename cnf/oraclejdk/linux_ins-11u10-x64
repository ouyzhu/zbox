# oracle need accept the license on the web page, which fails the wget, NEED download and create std link manually
ins_src_addr = https://fake_address_since_need_agreement/jdk-11.0.10_linux-x64_bin.tar.gz
