ins_steps           = "src ucd move"
ins_pre_script      = 'command -v java'
ins_pre_script_desc = "java must exist in PATH"

		    # proguard is NOT added into PATH
use_env             = 'ANDROID_SDK_ROOT=ZBOX_INS_FULLPATH PATH=${ANDROID_SDK_ROOT}/:${ANDROID_SDK_ROOT}/bin/:${PATH} '
